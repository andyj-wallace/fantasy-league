Assuming:
    Simple UI
    Single league type (what we've designed)
    One experienced full-stack engineer
    Existing football data provider
    No native mobile apps
    No fancy live updates
    No payment system
    Aggressive MVP

8–12 weeks
    Weeks 1–2
        Database design
        Backend foundation
        Authentication
        League creation/joining
    Weeks 3–4
        Squad builder
        Transfers
        Formation validation
        Captaincy
    Weeks 5–6
        Football data importer
        Player database
        Match database
        Locking logic
    Weeks 7–8
        Scoring engine
        Auto-subs
        Leaderboards
        Gameweek processing
    Weeks 9–10
        Testing
        Bug fixes
        Admin features
    Weeks 11–12
        Deployment
        Monitoring
        Beta testing


More Realistic (side project)
4–6 months

Most projects take longer because:
    Football data quirks
    Scoring edge cases
    Transfer bugs
    Auto-sub bugs
    Data quality issues


What actually takes the longest?

Not the frontend.
Not the APIs.
The scoring engine.

For example:

    Player starts match
    Gets injured after 8 minutes
    Receives yellow card
    Captain doesn't play
    Vice-captain does play
    Bench substitution occurs
    Match is later ruled postponed

Those edge cases consume a disproportionate amount of development time.


Milestone 0 — COMPLETED. Playable internally.
    Login
    Create league
    Join league
    Build squad
    Save lineup
    Leaderboard
    Transfers
    Player detail
    (No scoring in this milestone.)

Milestone 1 — COMPLETED. Game actually works (2026-07-01).
    All blocking items resolved. Backend scoring, locking, race conditions, price updates,
    and live API integration validated end-to-end.

    Dev testing completed (follow this order strictly):
        [done] Strategy 1: src/db/seedMatchStats.ts (npm run seed:match-stats) — inserts a
            synthetic Arsenal 2-0 Chelsea match, runs the full scoring cascade, and asserts
            11 hand-computed expected values (all passing). Zero API calls. Verified 2026-06-30.
        [done] Strategy 2: 6-gameweek smoke test with real player data, fixtures, and scoring.
            Created src/testing/seed6GameweekSmokeTest.ts (verified 2026-07-01). See docs/testing/
            for comprehensive findings on UX gaps (high-priority: season awareness, match schedule,
            scoring visibility; medium-priority: navigation, feedback, onboarding).
        Strategy 3 (deferred to M3): Single targeted live smoke test (≤10 real API calls, one-time only).
            Reserved for pre-Milestone 3 launch confirmation — never routine dev, never CI.

    Scoring engine correctness:
        [done] playerScores unique constraint on (playerId, matchId) — migration 0004, applied
        [done] Switch replaceForMatch to upsert

    Locking — API-level enforcement (all endpoints, P0):
        [done] makeTransfer: assertPlayersNotLocked guard, atomic transaction
        [done] setTeamRoster: detects locked players, rejects moves/removals, allows idempotent saves
        [done] setTeamLineup: rejects captain/VC changes to locked players, allows idempotent saves

    Race condition P0 fixes:
        [done] makeTransfer: wrapped in db.transaction() with FOR UPDATE lock on team row;
            added missing budget-negative check; DbOrTx threading through three repositories
        [done] incrementBankedFreeTransferCount: single atomic
            UPDATE ... SET banked_free_transfer_count = LEAST(banked_free_transfer_count + $1, $2)

    Monthly price-update formula:
        [done] Formula: 50% recent form (5 last matches) + 25% transfer activity (30d net) + 25% ownership
        [done] Implemented in src/domain/playerPricing.ts + runMonthlyPriceUpdate.ts
        [done] All constants tunable; weights independent

    First live worker cycle run:
        [done] Ran npm run db:migrate (migrations 0003, 0004, 0005) against real DB
        [done] Ran npm run dev:worker manually with real API-Football key
        [done] Validated: roster import, fixture discovery, scoring cascade all correct
        [done] Tested against 2024 season (API-Football's current populated season)

    See remaining-gaps-todo.md for full details.

Milestone 2 — Pre-production hardening. (Between game-works and production-ready.)
    Build order: complete Milestone 1 in full before starting here.
    In progress (2026-07-02): test suite + auth hardening done; race conditions + design
    decisions still open.

    Test suite:
        [done] Added vitest (vitest.config.ts, npm test / npm run test:watch). Pure tests —
            they mock the db/repositories barrel, so no Postgres needed. Tests co-located as
            src/**/*.test.ts; shared fixture builders in src/testing/fixtures.ts.
        [done] Covered calculatePlayerScores (full point table incl. GK/DEF-only clean sheet),
            calculateTeamScores (captain 2x + vice fallback, bench counts directly), and
            updateStandings (4-level tiebreaker + shared-rank/gap). Plus SignedTokenAuthProvider,
            credentialValidation, loginRateLimiter. 50 tests, all green (2026-07-02).
        [deferred] Wire Strategy 2 recorded fixtures as regression files for import-layer
            coverage — belongs with the OfflineFootballDataProvider import-layer work, not the
            scoring pass. Still open.

    Race condition P1/P2 fixes: (still open)
        Add UNIQUE(league_id, user_id) to teams table + upsert-or-409 in joinLeague
        providerPollState.getOrCreate: INSERT ... ON CONFLICT DO NOTHING then re-SELECT
        updateStandings: wrap read+compute in REPEATABLE READ transaction

    Auth hardening:
        [done] Signed, expiring session tokens — SignedTokenAuthProvider (HMAC over
            {userId, exp} keyed on AUTH_TOKEN_SECRET) replaces the forgeable stub.<userId>.
            Default in createAuthProviderFromEnv.ts; AUTH_PROVIDER=stub still available for
            scripts/tests. (2026-07-02)
        [done] Gated read endpoints behind requireAuth (getTeam, getAvailableTransfers,
            getLeague, getPlayer, searchPlayers, getLeagueStandings). Consumers already used
            authedFetch from client components except the player-detail page — converted it from
            a server component to a client component so it carries the stored token (rather than
            introducing cookie-based sessions this pass).
        [done] Login/check-email input validation (credentialValidation.ts) + per-account and
            per-IP rate limiting on /auth/login (loginRateLimiter.ts, in-memory sliding window;
            per-instance — move to a shared store if scaled horizontally).
        [deferred] Real credential check (Cognito). Login is still passwordless; only the token
            is hardened. This is the CognitoAuthProvider implements AuthProvider swap in
            createAuthProviderFromEnv.ts — no handler changes needed. Still open.

    Open design decisions:
        Confirmation-pass correction: decide whether a late VAR correction re-opens finalized
        standings (currently it recalculates PlayerScore rows only, not TeamScore/LeagueStanding)
        Roster diff semantics: define what happens when setTeamRoster/setTeamLineup is submitted
        with locked players whose values are unchanged vs. changed

    See remaining-gaps-todo.md items: 3, 7, 9 (P1-joins + P2)

Milestone 3 — Production ready.
    Build order: complete Milestone 2 in full before starting here.

    Deployment infra (AWS — CDK or Serverless Framework)
    Admin tools
    Monitoring and alerting
    Performance review
    Beta testing and bug fixing
    Full player roster hydration (requires paid API-Football plan —
        re-add importPlayerRoster to hydratePlayerRosterOnce, tune FOOTBALL_DATA_REQUEST_DELAY_MS)
    Revisit FOOTBALL_DATA_SEASON_YEAR once upcoming season data confirmed on API-Football
    Pre-launch: run Strategy 3 (single live API-Football smoke test — only real-quota run
        in the entire dev cycle)

    See remaining-gaps-todo.md item: 6, and any open items carried forward

Milestone 5 — Bonus Points: Game State Goals.
    Build order: complete Milestone 3 in full before starting here.

    Concept: goals that dramatically change the match outcome carry extra fantasy weight.
    The exact base bonus/penalty values are TBD (see fantasy_league_v1_design.txt); the
    framework below is fixed.

    Scenarios:
        Winning goal   — scorer + assister of the decisive winning goal get heavy bonus
        Equalizing goal — scorer + assister of any goal that ties a deficit get heavy bonus
        Losing goal    — GKs and DEFs of the conceding team, plus own-goal scorers, get
                         heavy penalty for the goal that sealed their loss

    Timing multiplier (applied to the bonus/penalty, not the base event points):
        0–75 min:   1.0× (no boost)
        76–80 min:  1.2×
        81–85 min:  1.6×
        86–90 min:  2.0×
        90+ min:    2.5× (all added time and extra time beyond 90)

    Open design decisions (must be resolved before implementation):
        [ ] Exact base bonus/penalty values for each scenario
        [x] "Equalizing goal" definition: ✅ only the final equalizer in a draw — mid-match
            ties subsequently broken do not qualify
        [ ] Losing-goal penalty scope: all GKs/DEFs on pitch at time of concession, or
            starters only?
        [ ] Own-goal as decisive goal: scorer takes the penalty; no assister; no positive
            bonus (own goals cannot "win" for the scoring side)

    Implementation tasks:
        [ ] Extend PlayerMatchStat / match event tracking to record goal_minute and
            compute goal_type (winning / equalizing / losing) post-match from score progression
        [ ] Add applyGameStateBonus step to calculatePlayerScores, after base event scoring
        [ ] Add timing multiplier helper (goalMinuteToTimingMultiplier) to domain/
        [ ] Update seedMatchStats.ts to cover game-state bonus scenarios and assert expected
            values for each time bracket
        [ ] Add vitest coverage: all three scenarios × all five time brackets = 15 test cases
            minimum; plus own-goal edge cases
